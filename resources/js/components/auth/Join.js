import React from 'react';
import PropTypes from 'prop-types';
import JoinPresenter from './JoinPresenter';

const Join = props => {
    return (
        <div>
            <JoinPresenter />
        </div>
    );
};

Join.propTypes = {};

export default Join;
